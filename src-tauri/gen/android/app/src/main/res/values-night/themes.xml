<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.folioapp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- System bars -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>

        <!-- Splash Screen attributes for Android 12+ (API 31+) -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">#1e1e1e</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@mipmap/ic_launcher</item>
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="s">#1e1e1e</item>

        <!-- Window background for pre-Android 12 -->
        <item name="android:windowBackground">@drawable/splash_background_night</item>
    </style>
</resources>
